"""Action surfaces. Empty at build step 1 by design — the spine is proved first.

files.py and apps.py arrive at step 2, browser.py at step 5, shell.py and comms.py
at step 6. The two that can do real damage are deliberately last.
"""
